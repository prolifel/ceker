services:
  mysql:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: ceker
      MYSQL_USER: ceker_user
      MYSQL_PASSWORD: ceker_pass
    ports:
      - "3306:3306"
    networks:
      - ceker-net
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  ceker:
    image: cr.prolifel.com/ceker:latest
    ports:
      - "3000:3000"
    networks:
      - ceker-net
      - nginx_proxy
    volumes:
      - screenshots-data:/app/public/screenshots
    environment:
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_DATABASE=${DB_DATABASE}
      - BROWSERLESS_API_TOKEN=${BROWSERLESS_API_TOKEN}
      - CLOUDFLARE_ACCOUNT_ID=${CLOUDFLARE_ACCOUNT_ID}
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN}
      - TEAMS_WEBHOOK_URL=${TEAMS_WEBHOOK_URL}
      - APP_URL=${APP_URL}
      - BOT_API_KEY=${BOT_API_KEY}
    depends_on:
      mysql:
        condition: service_healthy

  ceker-teams:
    image: cr.prolifel.com/ceker-teams:latest
    ports:
      - "3978:3978"
    networks:
      - nginx_proxy
    environment:
      - MicrosoftAppId=${MicrosoftAppId}
      - MicrosoftAppPassword=${MicrosoftAppPassword}
      - PORT=${PORT}
      - BOT_ENDPOINT_URL=${BOT_ENDPOINT_URL}
      - CHECK_API_URL=${CHECK_API_URL}
      - BOT_API_KEY=${BOT_API_KEY}
      - CTI_API_URL=${CTI_API_URL}

volumes:
  mysql-data:
  screenshots-data:
networks:
  ceker-net:
  nginx_proxy:
      external: true